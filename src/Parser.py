import sys
import json

""" Load Setting.json, and then generate the GPU device list """
def genGPUList(setting_file):
  setting = open(setting_file)

""" Generate the code """
def genExecuteFile(setting, meta_file):
  meta = open(meta_file)

""" Cut the meta file """
""" Keep the normal part """
""" Deal with the special part """
def cutFile():
  print "Cut"

